#include<stdio.h>
#define addnum(i,j) (i+j)
#define mul(a,b) (a)*(b)

int main() {
	printf("Sum of the numers is %d \n",addnum(5,9));
        printf("mul of the numers is %d ",mul(5+1,9-1));			
	return 0;

}

